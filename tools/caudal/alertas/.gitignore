# Datos generados por el motor: estado, cache de S3 y digests.
# No van al repo (es público y esto es salida, no código).
datos/

# Destinatarios reales (correos de clientes). El .ejemplo sí se versiona.
destinatarios.json

__pycache__/
*.pyc
